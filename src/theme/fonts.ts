import { Arvo, Gloria_Hallelujah } from 'next/font/google';

export const arvo = Arvo({
    weight: ['400'],
    subsets: ['latin'],
});

export const gloria = Gloria_Hallelujah({
    weight: ['400'],
    subsets: ['latin'],
});